# Hunter Run Tracker — Architecture

_A service that records and queries gate outcomes for hunter runs, enabling hunters to track their progress and optimize future hunts._

Generated by code-engine; this document derives from the system model in `.code_engine/graph.json`.

## Components

| Component | Kind | Purpose | Depends on |
|---|---|---|---|
| hunt_service | service | Handles hunt-related operations | database |
| gate_outcome_service | service | Handles gate outcome-related operations | database, hunt_service |
| auth_service | service | Handles authentication-related operations | — |
| csv_export_service | service | Handles CSV export-related operations | hunt_service |
| database | store | SQLite persistence via SQLAlchemy; session-per-request | — |
| web_ui | ui | dependency-free static frontend over the API | hunt_service, gate_outcome_service, auth_service, csv_export_service |

## Entities

- **HunterRun**: hunter_id:str, location:str, timestamp:datetime
- **GateOutcome**: hunter_run_id:int, gate_outcome:str

## Decisions

### Stack: FastAPI + SQLAlchemy/SQLite + pytest + static JS

Generate a modular FastAPI service with SQLite persistence, pytest verification, and a dependency-free static frontend.

_Rationale:_ Local-first constraint; single proven deep stack over shallow multi-stack pretense; every layer verifiable offline. Pattern priors considered: router_service_split, session_per_request, schema_in_schema_out, tests_per_feature

## Features → Requirements

- **create_hunt_entry** — Users can create a new hunt entry with gate outcomes (satisfies 1 requirement(s))
- **view_past_hunts** — Users can view all their past hunts and gate outcomes (satisfies 1 requirement(s))
- **store_gate_outcome_data** — Store gate outcome data with timestamp, location, and hunter ID (satisfies 1 requirement(s))
- **authenticate_users** — Implement authentication for users to access their hunt history (satisfies 1 requirement(s))
- **handle_concurrent_requests** — Ensure the service can handle 100 concurrent user requests without performance degradation (satisfies 1 requirement(s))
- **filter_and_sort_hunt_history** — Allow users to filter and sort their hunt history by date, location, or gate outcome (satisfies 1 requirement(s))
- **export_hunt_data_csv** — Users can export their hunt data in CSV format (satisfies 1 requirement(s))
