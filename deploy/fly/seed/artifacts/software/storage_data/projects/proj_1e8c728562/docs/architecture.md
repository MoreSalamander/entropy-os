# String Reverser — Architecture

_Provides a simple API for reversing strings, useful in text processing and data manipulation._

Generated by code-engine; this document derives from the system model in `.code_engine/graph.json`.

## Components

| Component | Kind | Purpose | Depends on |
|---|---|---|---|
| string_reverser_service | service | Reverse input string with correct character order | database |
| input_limiter_service | service | Prevent buffer overflow attacks by limiting input length | string_reverser_service |
| debugging_service | service | Store input string for later retrieval (e.g., debugging purposes) | string_reverser_service, input_limiter_service |
| performance_monitor_service | service | Achieve performance of reversing strings in under 10ms for typical inputs | string_reverser_service, input_limiter_service |
| unicode_support_service | service | Support Unicode characters and non-ASCII encodings | string_reverser_service, input_limiter_service |
| database | store | SQLite persistence via SQLAlchemy; session-per-request | — |
| web_ui | ui | dependency-free static frontend over the API | string_reverser_service, input_limiter_service, debugging_service, performance_monitor_service, unicode_support_service |

## Entities

- **StringReversalRequest**: input_string:str
- **StringReversalResponse**: reversed_string:str

## Decisions

### Stack: FastAPI + SQLAlchemy/SQLite + pytest + static JS

Generate a modular FastAPI service with SQLite persistence, pytest verification, and a dependency-free static frontend.

_Rationale:_ Local-first constraint; single proven deep stack over shallow multi-stack pretense; every layer verifiable offline. Pattern priors considered: router_service_split, session_per_request, schema_in_schema_out, tests_per_feature

## Features → Requirements

- **reverse_string** — Reverse input string with correct character order (satisfies 0 requirement(s))
- **handle_various_lengths** — Handle strings of varying lengths, including empty and single-character inputs (satisfies 0 requirement(s))
- **store_input_string** — Store input string for later retrieval (e.g., debugging purposes) (satisfies 0 requirement(s))
- **prevent_buffer_overflow** — Prevent buffer overflow attacks by limiting input length (satisfies 0 requirement(s))
- **achieve_performance** — Achieve performance of reversing strings in under 10ms for typical inputs (satisfies 0 requirement(s))
- **support_unicode_characters** — Support Unicode characters and non-ASCII encodings (satisfies 0 requirement(s))
- **Cover: Reverse input string with correct character order** — Auto-created to satisfy uncovered MUST requirement: Reverse input string with correct character orde (satisfies 1 requirement(s))
