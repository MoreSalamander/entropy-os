# Hunter Run Outcome Tracker — Architecture

_Provide a service for recording and querying gate outcomes of hunter runs, utilizing the organisation's metadata catalog._

Generated by code-engine; this document derives from the system model in `.code_engine/graph.json`.

## Components

| Component | Kind | Purpose | Depends on |
|---|---|---|---|
| gate_outcome_service | service | Record and query gate outcomes of hunter runs | database |
| auth_service | service | Implement authentication and authorization | — |
| data_export_service | service | Support data export in standard format | gate_outcome_service |
| database | store | SQLite persistence via SQLAlchemy; session-per-request | — |
| web_ui | ui | dependency-free static frontend over the API | gate_outcome_service, auth_service |

## Entities

- **HunterRunOutcome**: hunter_run_id:str, gate_outcome:str, opportunity_type:str, model_invoked:str, created_at:datetime
- **Opportunity**: hunter_run_id:str, opportunity_type:str, created_at:datetime

## Decisions

### Stack: FastAPI + SQLAlchemy/SQLite + pytest + static JS

Generate a modular FastAPI service with SQLite persistence, pytest verification, and a dependency-free static frontend.

_Rationale:_ Local-first constraint; single proven deep stack over shallow multi-stack pretense; every layer verifiable offline. Pattern priors considered: router_service_split, session_per_request, schema_in_schema_out, tests_per_feature

## Features → Requirements

- **Record gate outcome data from hunter runs** — Functional requirement to record gate outcomes of hunter runs, including accepted and rejected oppor (satisfies 1 requirement(s))
- **Query recorded gate outcomes by filters** — Functional requirement to allow users to query recorded gate outcomes by various filters (e.g., oppo (satisfies 1 requirement(s))
- **Store data in structured format** — Data requirement to store data in a structured format, mirroring the organisation's metadata catalog (satisfies 1 requirement(s))
- **Implement authentication and authorization** — Security requirement to implement authentication and authorization to restrict access to recorded ga (satisfies 1 requirement(s))
- **Ensure high volume of concurrent queries performance** — Non-functional requirement to ensure the service can handle a high volume of concurrent queries with (satisfies 1 requirement(s))
- **Support data export in standard format** — Data requirement to support data export in a standard format (e.g., CSV) for further analysis or pro (satisfies 1 requirement(s))
- **Automated test suite covering every feature** — Non-functional requirement to have an automated test suite covering every feature. (satisfies 1 requirement(s))
