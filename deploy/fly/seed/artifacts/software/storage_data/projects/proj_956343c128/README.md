# GPUcademy 2.0

A comprehensive online platform for learning WebGPU compute shaders, providing an updated curriculum with a focus on recent changes and advancements in the field.

Generated by [code-engine](https://github.com/MoreSalamander/code-engine). The repository carries its own semantic model in `.code_engine/graph.json` — impact analysis and evolution checks read it.

## Run

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Test

```bash
pip install -r requirements-dev.txt
pytest
```

## Honest notes

- Custom endpoints marked `implemented: false` are visible stubs awaiting real logic.
- See `docs/architecture.md` for components and decisions.
