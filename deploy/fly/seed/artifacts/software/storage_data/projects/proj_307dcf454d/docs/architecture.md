# DollarWise — Architecture

_An interactive web platform that teaches adult beginners about U.S. currency through a structured curriculum, promoting financial literacy and independence._

Generated by code-engine; this document derives from the system model in `.code_engine/graph.json`.

## Components

| Component | Kind | Purpose | Depends on |
|---|---|---|---|
| lesson_service | service | manages lesson content and quizzes | database |
| user_service | service | manages user accounts and progress tracking | database |
| quiz_service | service | manages quizzes for concepts | lesson_service |
| goal_service | service | manages financial goal tracking | — |
| database | store | SQLite persistence via SQLAlchemy; session-per-request | — |
| web_ui | ui | dependency-free static frontend over the API | lesson_service, user_service |

## Entities

- **User**: username:str, password_hash:str, email:str
- **LessonContent**: title:str, description:text, multimedia_content:str
- **Quiz**: concept_id:int, question:str, answer:bool
- **UserProgress**: user_id:int, concept_id:int, mastery_level:float

## Decisions

### Stack: FastAPI + SQLAlchemy/SQLite + pytest + static JS

Generate a modular FastAPI service with SQLite persistence, pytest verification, and a dependency-free static frontend.

_Rationale:_ Local-first constraint; single proven deep stack over shallow multi-stack pretense; every layer verifiable offline. Pattern priors considered: router_service_split, session_per_request, schema_in_schema_out, tests_per_feature

## Features → Requirements

- **access_lesson_content** — Users can access lesson content pages with interactive multimedia content (e.g., videos, images, ani (satisfies 0 requirement(s))
- **quizzes_for_concepts** — Quizzes are created for each concept, allowing users to test their knowledge and receive graded feed (satisfies 1 requirement(s))
- **user_progress_tracking** — User progress tracking is implemented, including mastery levels per concept and overall completion s (satisfies 1 requirement(s))
- **secure_user_accounts** — User accounts are created with secure login credentials (e.g., password hashing, salted storage) (satisfies 0 requirement(s))
- **responsive_design** — The platform is accessible on desktop and mobile devices, with a responsive design for optimal user  (satisfies 1 requirement(s))
- **lesson_content_updates** — Lesson content pages are updated regularly to reflect changes in U.S. currency policies or regulatio (satisfies 1 requirement(s))
- **financial_goal_tracking** — Users can set and track financial goals, with personalized recommendations for achieving them (satisfies 1 requirement(s))
- **Cover: Users can access lesson content pages with interac** — Auto-created to satisfy uncovered MUST requirement: Users can access lesson content pages with inter (satisfies 1 requirement(s))
