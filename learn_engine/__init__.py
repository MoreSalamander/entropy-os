"""learn-engine — AI-native Education Intelligence Platform.

Fourth engine on the research-engine substrate:

    Student Goal → Goal Analysis (roadmap DAG) → Parallel Educational
    Research → Student Context Graph → Education Knowledge Graph
    → Teaching Agent Organization → Adaptive Learning Engine
    → Practice + Assessment → Mastery Tracking → Improved Teaching Memory

Design law (family-wide): LLMs propose through schema gates; deterministic
code decides. Applied to education that means: mastery is computed from
graded evidence (never asserted), the curriculum is a topological walk of a
validated prerequisite DAG (never a prose plan), review timing is a
deterministic schedule, and generated code exercises are executed before
they are ever shown to a learner — an exercise whose reference solution
does not reproduce its claimed output is rejected at the gate.
"""

__version__ = "0.1.0"
