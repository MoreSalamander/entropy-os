# Core runtime — everything here is required and runs fully local.
fastapi>=0.115
uvicorn>=0.32
httpx>=0.27
pydantic>=2.10
PyYAML>=6.0
networkx>=3.4
qdrant-client>=1.12        # used in EMBEDDED local mode (path=...), no server required
SQLAlchemy>=2.0
aiosqlite>=0.20
feedparser>=6.0            # arXiv Atom feeds
beautifulsoup4>=4.12       # webpage text extraction

# Tests
pytest>=8.3
pytest-asyncio>=0.24

# Optional backends — the spec's server stack. Uncomment when the server exists.
# neo4j>=5.25              # GraphStore flip: graph.backend=neo4j
# redis>=5.0               # QueueBackend flip: queue.backend=redis
# psycopg[binary]>=3.2     # relational flip: db.url=postgresql+psycopg://...
greenlet>=3.1            # SQLAlchemy asyncio bridge (not auto-installed on 3.14)
