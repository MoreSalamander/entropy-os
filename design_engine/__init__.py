"""design-engine — AI-native Website Intelligence and Generation Engine.

An AI web design organization built on the research-engine substrate:

    User Idea → Intent Analysis → Parallel Semantic Web Intelligence
    → Design Context Graph → Design Knowledge Graph (DataHub-emitted)
    → Design Synthesis → Website Generation (Next.js/React/TS/Tailwind)
    → Autonomous Review + Improvement → Graph Memory Loop

Design law (deterministic-scaffold thesis, inherited from research-engine):
LLMs propose — design directions, section plans, copy — through schema
gates; deterministic code decides and renders. The component library IS the
code generator: analyzed websites contribute abstract traits (palette roles,
type classes, layout archetypes), never markup, so originality is enforced
by construction, not by promise.
"""

__version__ = "0.1.0"
